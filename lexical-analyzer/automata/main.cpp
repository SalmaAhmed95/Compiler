
#include "Automata.h"
#include "NFA.h"
#include <iostream>

/*int main() {
  NFA *automata = new NFA();
  stateID node1ID = automata->createNode();

  stateID node2ID = automata->createNode();
  stateID node3ID = automata->createNode();

  automata->addTransition('a', 0, 1);
  automata->addTransition('a', 0, 2);
  std::string id("keyword");
  automata->createNode(ACCEPTED, 0, id);
 // std::cout << automata->getIdentifier(3);

  return 0;
}
*/

//#include "Automata.h"
//#include "NFA.h"
//#include <iostream>
//
// int main() {
//  NFA *automata = new NFA();
//  stateID node1ID = automata->createNode();
//
//  stateID node2ID = automata->createNode();
//  stateID node3ID = automata->createNode();
//
//  automata->addTransition('a', 0, 1);
//  automata->addTransition('a', 0, 2);
//  std::string id("keyword");
//  automata->createNode(ACCEPTED, 0, id);
// // std::cout << automata->getIdentifier(3);
//
//  return 0;
//}
